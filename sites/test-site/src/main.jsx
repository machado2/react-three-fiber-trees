import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'
import { Physicstree } from 'react-three-fiber-trees'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Physicstree />
  </React.StrictMode>,
)
